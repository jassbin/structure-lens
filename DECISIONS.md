# 结构透镜（Structure Lens）· 产品决策与思路沉淀

> 本文档沉淀整个共创过程中的**方法论标尺、关键设计决策、注意点、当前边界与未来考量**。
> 目的：让任何接手者（或未来的自己）不必翻聊天记录，就能理解"为什么是现在这样"以及"下一步该往哪走"。
> 维护约定：每次做出影响产品形态的决策，追加到对应章节，并在文末「变更日志」记一行。

---

## 0. 一句话定位

一个**单事件深度穿透器**：贴入一个值得挖的事件，AI 严格按一套可信、可错、可进化、可复用的分析方法论，把它穿透到底层结构，产出一句命名式金句结论 + 8 步可见推理 + 一张可沉淀复用的结构骨架卡。

---

## 1. 方法论标尺（判断"做得对不对"的唯一依据）

产品的分析逻辑**不是凭空设计的**，而是对齐两份方法论 skill：
- `deep-analysis`（v3）：14 个方法（00–13）+ 两条主路由（A 单事件垂直穿透 / B 结构溯因游走）。
- `structural-navigator`：结构溯因游走法，两个哲学前提 + 三类根结构（提取-分配 / 委托-执行 / 权力竞争-均衡）+ S01–S14 方法。

**用户概括的方法论主线（已确认正确）：**
> 发现异常点 → 剥离道德叙事 → 向下钻探深挖 → 挖到一个结构 → 与同构事件碰撞校验 → 自我反驳。

**四条"护城河"（区别于高级阴谋论的关键，都已产品化）：**
1. **可信** —— 信源分级（strong/medium/weak/unverifiable），地基。异常点若是假的，越挖越错。
2. **可错** —— 可证伪条件，让结论能被打脸，而不是万能解释。
3. **可进化** —— 人机共同推演 + 增量留痕，结论被质疑推着优化。
4. **可复用** —— 结构命名 + 迁移，攒出结构库，看下一件事更快。

**关键区分（必须守住，来自 skill 的执行原则）：**
- 事件抽象（中性骨架）**必须先于**暗黑穿透。先看形状，再照颜色。
- 局部暗黑（单事件）vs 整体暗黑（全结构地图）是两个操作，后者不能前置。
- 游走被**张力驱动**，不被计划驱动。
- 结构同构 ≠ 表面相似。
- 异常点是洞见来源，框架只是容器。
- **自反检验**：证据分级/元认知审计这套标准，必须同样施加于 AI 自己——无法诚实提供来源的细节要标"无法验证"，不许为了显得完整而编造。

---

## 2. 初版边界（已锁定）

**初版 = 路由 A「单事件穿透器」。8 步不再加新能力，把现有链路打磨到体验/颜值拉满。**

### 已产品化（8 步流水线，对应 skill 方法）
| 步 | 名称 | 对应 skill 方法 |
|---|---|---|
| 0 | 材料与信源分级 | S13 证据分级锁定 |
| 1 | 异常锁定（杠杆率排序） | 00 异常点驱动 |
| 2 | 中性骨架 | 11/S02 事件抽象骨架 |
| 3 | 机制穿透（下三钻·递归钻探链） | 03/S03 三层下钻 + 01 局部暗黑（隐式糅进爆破点） |
| 4 | 博弈与类比 | 04 博弈建模 + 06/S08 跨域迁移 |
| 5 | 情景分支 | S14 情景扩展 + 02 概率锚定 |
| 6 | 核心判断 + 可证伪 | 06 核心判断 + 可证伪 |
| 7 | 对抗质检 | 08 魔鬼代言人 + 10/S12 元认知审计 |

外加：结构骨架卡（可沉淀复用）、结构地图（本地+云端）、人机辩论「我不同意」全链路。

### 诚实承认的遗落（v2 明确延后，不是漏）
- **整条路由 B（结构游走维度）基本没做**：S05 张力识别（游走引擎）、12/S06 结构游走导航、S07 结构对比激活、S09 系统化建图、13/S10 整体暗黑扫描。当前只有"游走钩子"占位。这是 v3 的核心升级，但依赖多事件积累，属于"用久才涌现价值"的重维度，初版不塞是合理取舍。
- **弱化的中频/质控件**：09 目标逆推（单一强力行动者意图建模）缺；01 局部暗黑没独立成步；05 框架漂移校正弱；S11 结构验证只有"可证伪条件"沾边。

---

## 3. 关键设计决策（含"为什么"）

### 3.1 机制穿透 = 递归钻探链（不是三段平铺）
- **问题**：原来 surface/deep/bottom 是三个并列文本框 → 写成平铺陈述，没有"层层深入、有爆破"的钻探感。
- **决策**：改为 `anchorAnomaly`（锁定异常）+ `layers[{ask, finding, breakthrough}]`（逐层 why 追问，每层强制反直觉爆破点）+ `bedrock/bedrockKind`（钻到人性/激励/权力/信息/稀缺基岩为止，2–5 层由结构深度决定）。
- **要点**：① 钻探必须**顺着异常锁定选中的那个异常点**往下，不许跑题；② 每层没有反转/爆破不算有效钻探，宁少一层别灌水。

### 3.2 人机辩论「我不同意」+ 增量不覆盖
- **原则**：用户反驳 → AI 先表态（absorb 吸收 / compromise 折中 / hold 保持），任何表态都必须给理由。
- **增量不覆盖**：采纳/折中时，**不静默替换原内容**，而是保留原文、在下面追加"覆盖层"（标注原内容因何不再适用 obsoleteReason + 新增 addendum）。忠实反映人机思维碰撞推动的变化。
- hold 时零改动，只留一条辩论痕迹（version 不递增）。

### 3.3 结构骨架卡也参与动态优化（三种入口统一）
骨架卡是结论枢纽，必须跟着讨论动态优化并**留记录**，不能"过程改了、结论没动"：
1. **直接反驳骨架卡** → 骨架追"本次调整"overlay（含 root 改判留痕）。
2. **反驳下游步骤且 AI 判断波及结论** → 骨架追一条**联动留痕**，标注 `triggeredBy`（"因你对【机制穿透】的反驳，本结论随之更新"）。← **智能阈值**：AI 判断不波及就不硬塞。
3. hold / 不波及 → 骨架不动（合理，不是漏）。

### 3.4 分诊：短输入先联网、后对齐
- 短于阈值的输入不一刀切反问：**先默默联网搜**。
  - 搜到 → 生成中性事实概要 + 来源，弹「事实对齐卡」让用户确认/编辑，确认后透传分析。
  - 搜不到 → 退回 ProbePanel 反问引导。

### 3.4b 搜索优化：从"原来不行"到"现在行了"
- **原来的问题**：联网搜索（DuckDuckGo）经常被反爬/限流打回空结果，导致"先联网后对齐"这套逻辑形同虚设——短输入要么搜不到、要么直接崩，只能退回反问，事实对齐卡几乎不触发。
- **现在的状态（已修复，实测可用）**：`ddgSearch` 加了防反爬处理，短输入能稳定命中真实来源。实测"孙宇晨和景甜事件"这类具体时事，precheck 状态为 `align`、命中多条真实来源、生成中性事实概要，用户确认后直接透传分析。整条"短输入→静默搜→事实对齐卡→透传"链路端到端跑通。
- **注意**：搜索结果只作事实对齐用，AI 被要求"仅依据检索资料写中性概要，绝不添加资料里没有的人名/金额/时间/因果"。搜索失败时静默降级（`.catch(() => [])`），不打断分析主流程。

### 3.5 动态零件（骨架卡）
- 骨架零件**不写死五格**，由 AI 按结构本身决定 2–5 个（`parts: {label,value}[]`）。提取型/委托型/权力型各不相同。避免"套模板"式的假中性。

### 3.6 登录边界：免登录可用，登录才云端
- **免登录**：首页输入、分诊/对齐、8 步报告（含下三钻/辩论/重算）、结构地图查看 —— 全部可用、可互动，数据存 localStorage。
- **登录**：仅当用户想**把结果存云端 / 跨设备**时。
- **踩过的坑**：曾用 `AuthGate` 把 `/analysis/[id]` 和 `/map` 整个包成登录墙 → 未登录分析完跳报告页被挡死，与产品意图完全相反。**已删除 AuthGate**，两页改为免登录。

### 3.7 结构地图 = 本地与云端**双向**同步（并集，缺的都补齐）
- **意图**：前面所有分析才是最重要的资产，一条都不能丢。登录后本地与云端取**并集**。
- **实现**：`/api/structure-map/sync` —— ① 本地有、云端没有的 push 上云（按 id 判重，避免重复累计 hits）；② 返回云端全量，前端把云端有、本地没有的**回流写入 localStorage**；③ `rebuildLocalMapFrom` 用全量分析**幂等重建**本地地图（不重复累计）。
- 登录后自动同步一次；地图页按钮也可手动触发。同步失败退回云端只读展示，不损坏本地数据。

---

## 4. 注意点 / 硬约束（改代码前必读）

- **i18n**：所有面向用户文案走 `react-i18next`，`en-US.json` / `zh-CN.json` 两边键必须对齐（有校验脚本）。带变量用 `{{count}}` 插值，别用字符串模板拼。当前对话默认简体中文。
- **诚实高于完整**：无法核实标 unverifiable，不编造数字/人名/时间。
- **结构命名**要揭示本质、是关系命题、可迁移（差："王安石变法失败"；好："用来改革中间层的工具，本身就是中间层"）。
- **旧数据兼容**：改数据结构时，validate 层必须对 localStorage 里的历史版本做回退映射（已对 骨架 parts、机制 surface/deep/bottom→layers 做过），别让老报告崩。
- **重算判重**：云端同步按 analysis id 判重，防止 mergeStructure 重复累计 hits 把假设误点亮成 verified。
- **375px 布局**：无横向溢出；下钻缩进用 `Math.min(i,3)*8` 并 `min-w-0` 防溢出。
- **反 AI slop**：图标用 lucide（如爆破点用 Zap），不用 emoji 堆砌；克制装饰。
- 分析全链路 API（analyze/precheck/drill/recompute）用 `getOptionalUser`，**不得**加 `requireAuth` 硬拦截；只有云端读写路由才 requireAuth。

---

## 5. 未来考量（v2+ 路线，按价值排序）

1. **启动路由 B（结构游走维度）** —— 最大的价值增量：
   - S05 张力识别：从骨架里找"解释不了的部分/成立条件"，作为游走方向。
   - S06 结构游走导航：张力引导跳到结构同构（非表面相似）的下一个事件。
   - S07 对比激活 + S09 系统化建图：把"游走钩子"升级成真正的跨事件结构地图（节点+边）。
   - 13/S10 整体暗黑扫描：对完整结构地图做全局元逻辑提取（独立步骤，游走完才做）。
2. **补中频战略件**：09 目标逆推（单一强力行动者场景）、01 局部暗黑独立成步（骨架有道德叙事张力时）。
3. **补质控件**：05 框架漂移校正随时触发；S11 结构验证补齐（不只可证伪条件）。
4. **下载/导出**：用户导出自己的结构地图 / 单份报告（PDF 或结构化 JSON）。
5. **hold 态的视觉凸显**：辩论里"保持不变"的有力理由值得更强呈现。
6. **materials 信源判断收紧**：强制摘要中不存在的信息必须标 unverifiable。

---

## 6. 数据与同步语义速记

- 本地存储键：`structure-lens:local-map`（结构节点）、`structure-lens:local-analyses`（整份分析 map）。
- 点亮阈值：同名结构 hits ≥ 2 且 events ≥ 2 → verified。
- **云端同步的是「整份分析的完整体」，不是摘要**：`analyses` 表逐字段存 `input / version / sources / verdict / steps（完整 8 步，含机制钻探链、每步 debate、下游 overlays）/ skeleton（含 parts、三段式、联动留痕 overlays、debate）/ revisions（人机辩论全记录）/ createdAt`。跨设备拉回后能完整重放 8 步、看到所有辩论与增量修订。
- 结构地图**节点**（结构名/root/hits/verified）不额外冗余存储，而是从完整分析折叠计算；同步全量分析后靠 `rebuildLocalMapFrom` 幂等重建，两端一致。
- 同步 = 并集：`push 本地缺失 → 拉云端全量 → 回流本地缺失 → 幂等重建本地地图`。

---

## 变更日志
- 初版方法论校验：对齐 deep-analysis / structural-navigator，锁定初版=路由A。
- 机制穿透重构为递归钻探链（锁定异常+逐层追问+爆破点+见底基岩）。
- 骨架卡增量不覆盖 + 下游反驳智能阈值联动留痕（triggeredBy）。
- 分诊先联网后对齐（事实对齐卡）。
- 搜索优化：ddgSearch 防反爬，短输入稳定命中真实来源（原来不行现在行了）。
- 动态零件（骨架 parts 由 AI 决定 2–5 个）。
- 移除 AuthGate，报告页/地图页改为免登录。
- 结构地图改为本地⇄云端双向同步（并集，缺的都补齐）；云端存整份完整推理。
- 结构地图节点的历史事件做成可点击入口（复用现有分析，反查 title→id 跳报告页），零冗余。
- 底部「分析」tab 记忆最后停留的报告页（sessionStorage），从地图切回不丢原页面。
- 新增本文件 DECISIONS.md 沉淀思路。

## 变更日志（辩论与穿透逻辑优化 · 应对"应付感/跑偏"）
- **辩论 stance 阈值客观化**：不新增"完全认同"档（附和=讨好=逻辑陷阱）；改为判定只认"反驳有无客观依据"。compromise 从严——只有"一部分依据成立、一部分不成立"才可用，且必须逐条拆清「接受哪块+依据 / 保留哪块+依据」，拆不出就该选 absorb 或 hold。absorb 需说清推翻了原哪一步；hold 需先承认对的部分再解释为何不足以推翻；新增"用户自曝缺陷/自我否定"的客观回应规范（给真实反馈不给情绪安慰）。
- **下三钻不焊死单线**：主锚防跑偏（第一层 ask 须引用异常锁定选中那条的原文关键词，每层自检"是否仍在解释主异常"）；新增 sideAnomalies 旁生异常——钻探途中撞见的其他强异常"只标记不展开"（内容+为何值得另开一钻），兼顾深度与广度。types/validate/step-body/i18n 全链路联动，旧报告向后兼容。
- **三按钮各自锐化且不得复述正文**：deeper=顺这条延续下三钻再钻一层；challenge=依据审计（分清事实/推断/无法证实，指出最脆弱一环）；counter=最强反面解释（完整对立论证+在何条件下胜过原判断）。三者硬约束：输出必须是新增信息，重复正文即视为没钻进去。
- **首页推荐事件**：从示例题改为"最近一年有热度且表层≠深层"的真实事件（供演示穿透"表面叙事 vs 底层结构"的落差）。
