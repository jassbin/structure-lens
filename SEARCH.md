# 事实对齐与联网搜索说明

「结构透镜」在深度分析前，会先做一步**事实对齐**：把你输入的事件联网检索一遍，整理成客观概要 + 来源，让你确认或修正后再穿透。这样能避免"你说的事件"和"AI 理解的事件"不是同一件。

## 搜索源与已知限制

- **搜索源**：免 key 的 DuckDuckGo（`lite` / `html` 端点），服务器端通过系统 `curl`（HTTP/1.1）抓取，规避了 Node fetch 被判为异常流量（202 反爬页）的问题。
- **对英文/国际时事、公司、政策事件**：召回稳定、概要准确、附带真实来源链接。
- **对非常新的、或中文小众的时事/八卦**：召回不稳定。原因是运行环境是**海外数据中心 IP**：
  - 百度直接 302 反爬，拿不到结果；
  - Bing 从该 IP 常返回缓存兜底页；
  - DuckDuckGo 的免 JS 端点中文索引本就残缺，且偶发限流。
  - 你在自己浏览器里能搜到，是因为你的 IP、地区身份和完整 JS 环境，这些服务器端无法复制。

> 结论：免 key 抓 HTML 这条路，对中文最新事件**时灵时不灵**。这是网络位置 + 反爬的硬限制，不是代码缺陷。若需要对中文时事稳定检索，需接入带 key 的搜索 API（Tavily / Serper / Bing Web Search），它们从自身出口检索，不受本环境 IP 影响。

## 优雅降级：搜不到时怎么办

当联网**没有检索到任何相关资料**时，系统不会让 AI 硬编一句"无法确认"，而是：

1. `/api/align` 直接返回 `needsManual: true`（不消耗 AI），概要为空。
2. 前端弹出「没搜到实时资料，请手动对齐」卡片，引导你**粘贴事件经过、新闻摘要或链接**。
3. 你填好内容 → 点「用我填的内容开始穿透」→ 照常进入深度分析。

也就是说：**搜索只是加速事实对齐的可选前置步骤；搜不到时，你手动补一段概要，分析能力完全不受影响。**

## 相关实现

- 搜索抓取与解析：`src/lib/analysis/search.ts`
- 对齐路由（含 needsManual 降级）：`src/app/api/align/route.ts`
- 分析路由（接收对齐概要；有概要时用概要做可挖掘性分诊）：`src/app/api/analyze/route.ts`
- 对齐/手动对齐 UI：`src/components/home/align-card.tsx`
