# 联网搜索说明（静默模式）

「结构透镜」在深度分析时，会在**后台静默**联网检索一次，用来对齐事实——整个过程对用户不可见、不打断，也不需要任何确认步骤。

## 工作方式

1. 用户输入事件 → 点「开始穿透」→ 直接进入分析。
2. 服务端在 `/api/analyze` 内部先调 `ddgSearch(input)`：
   - **搜到相关资料**（多为"具体事件"，如某公司/政策/人物的真实新闻）：把资料作为"仅供对齐事实"的上下文喂给分析模型，并把 2-4 条来源存入结果（`sources`），报告页可展示。
   - **没搜到**（多为"通用现象 / 假设性命题"，如"某城市房产限购推高稀缺性"这类没有具体主体的结构描述）：**直接分析，不做任何提示**。这类输入本就不需要联网。
3. 无论搜没搜到，用户体验都是"输入 → 一步出报告"，不会再弹"请手动对齐"之类的打扰。

## 搜索源与已知限制

- **搜索源**：免 key 的 DuckDuckGo（`lite` / `html` 端点），服务器端通过系统 `curl`（HTTP/1.1）抓取，规避 Node fetch 被判为异常流量（202 反爬页）的问题。
- **对英文/国际时事、公司、政策事件**：召回较稳，能带回真实来源链接。
- **对非常新的、或中文小众的时事**：召回不稳定（运行环境是海外数据中心 IP，百度 302 反爬、Bing 常返回缓存页、DDG 中文索引残缺且偶发限流）。
- 因为搜索是**静默、可选、搜不到就跳过**的，这些限制不会影响分析本身——分析能力完全独立于搜索。若日后需要对中文时事稳定检索，可接入带 key 的搜索 API（Tavily / Serper / Bing Web Search），从服务自身出口检索，不受本环境 IP 影响。

## 相关实现

- 搜索抓取与解析：`src/lib/analysis/search.ts`
- 分析路由（内置静默搜索）：`src/app/api/analyze/route.ts`
- 分析系统提示词：`src/lib/analysis/prompt.ts`（已注明"附带资料仅供对齐、无关可忽略"）
